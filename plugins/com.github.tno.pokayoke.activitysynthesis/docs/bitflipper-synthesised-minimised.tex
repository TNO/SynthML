\begin{algorithm}[t!]
\lstset{language=Cif,
stringstyle=\ttfamily\color{red!50!brown},
showstringspaces=false}
\footnotesize
	\begin{lstlisting}
controllable Flipper;
controllable ND_BitInit;
controllable __start;
controllable __end;
automaton minimal:
  alphabet Flipper, ND_BitInit, __start, __end;
  @state(projected = "s1")
  location s1:
    initial;
    edge __start goto s2;
  @state(projected = "s2")
  location s2:
    edge ND_BitInit goto s4;
  @state(projected = "s4")
  location s3:
    marked;
  @state(projected = "s3")
  location s4:
    edge __end goto s3;
    edge Flipper goto s5;
  @state(projected = "s5")
  location s5:
    edge __end goto s3;
end
\end{lstlisting}
	\caption{Minimised CIF model. }
	\label{alg:bitflipper-minimised-cif}
\end{algorithm}